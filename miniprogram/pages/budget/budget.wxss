.budget-page {
  padding-bottom: 48rpx;
}

.hero {
  border: none;
  background:
    linear-gradient(145deg, rgba(11, 61, 46, 0.96), rgba(31, 107, 79, 0.88)),
    #0B3D2E;
  color: #fff;
}

.hero-kicker {
  font-size: 22rpx;
  letter-spacing: 2rpx;
  opacity: 0.75;
}

.hero-title {
  margin-top: 12rpx;
  font-size: 44rpx;
  font-weight: 700;
}

.hero-desc {
  margin-top: 10rpx;
  font-size: 24rpx;
  opacity: 0.82;
  line-height: 1.5;
}

.amount-panel {
  margin-top: 28rpx;
}

.field-label {
  display: block;
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-bottom: 16rpx;
}

.amount-row {
  display: flex;
  align-items: baseline;
  gap: 8rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid var(--color-border);
}

.currency {
  font-size: 40rpx;
  font-weight: 650;
  color: var(--color-brand);
}

.amount-input {
  flex: 1;
  height: 88rpx;
  font-size: 56rpx;
  font-weight: 700;
  color: var(--color-text);
}

.presets {
  margin-top: 28rpx;
}

.hint {
  margin-top: 24rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
}

.hint-em {
  color: var(--color-expense);
  font-weight: 600;
}

.tip-panel {
  margin-top: 28rpx;
}

.tip-title {
  font-size: 28rpx;
  font-weight: 600;
  margin-bottom: 12rpx;
}

.tip-item {
  position: relative;
  padding-left: 24rpx;
  margin-top: 10rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
  line-height: 1.55;
}

.tip-item::before {
  content: '';
  position: absolute;
  left: 0;
  top: 14rpx;
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  background: var(--color-brand-soft);
}

.save-btn {
  margin-top: 40rpx;
  width: 100%;
}
